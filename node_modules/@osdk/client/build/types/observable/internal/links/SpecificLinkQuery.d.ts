import type { ObjectTypeDefinition, Osdk, PageResult } from "@osdk/api";
import { type Subject } from "rxjs";
import type { SpecificLinkPayload } from "../../LinkPayload.js";
import type { Status } from "../../ObservableClient/common.js";
import type { ObserveLinks } from "../../ObservableClient/ObserveLink.js";
import { BaseListQuery } from "../base-list/BaseListQuery.js";
import type { BatchContext } from "../BatchContext.js";
import type { CacheKey } from "../CacheKey.js";
import type { Changes } from "../Changes.js";
import type { Entry } from "../Layer.js";
import type { OptimisticId } from "../OptimisticId.js";
import type { Store } from "../Store.js";
import type { SubjectPayload } from "../SubjectPayload.js";
import type { SpecificLinkCacheKey } from "./SpecificLinkCacheKey.js";
/**
* Query implementation for retrieving linked objects from a specific object.
* - Stores links as ObjectCacheKey[] references
* - Creates indirect dependencies on linked objects
* - Supports filtering and sorting of linked collections
* - Handles proper invalidation of related objects
*/
export declare class SpecificLinkQuery extends BaseListQuery<SpecificLinkCacheKey, SpecificLinkPayload, ObserveLinks.Options<ObjectTypeDefinition, string>> {
	#private;
	/**
	* Register changes to the cache specific to SpecificLinkQuery
	*/
	protected registerCacheChanges(batch: BatchContext): void;
	constructor(store: Store, subject: Subject<SubjectPayload<SpecificLinkCacheKey>>, cacheKey: SpecificLinkCacheKey, opts: ObserveLinks.Options<ObjectTypeDefinition, string>);
	/**
	* Implements fetchPageData from the BaseCollectionQuery template method pattern
	* Fetches a page of linked objects
	*/
	protected fetchPageData(signal: AbortSignal | undefined): Promise<PageResult<Osdk.Instance<any>>>;
	/**
	* Removes a link query from the store
	*/
	deleteFromStore(status: Status, batch: BatchContext): Entry<SpecificLinkCacheKey> | undefined;
	/**
	* Implements Query.maybeUpdateAndRevalidate to handle cache invalidation
	*/
	maybeUpdateAndRevalidate: (changes: Changes, _optimisticId: OptimisticId | undefined) => Promise<void>;
	invalidateObjectType: (objectType: string, changes: Changes | undefined) => Promise<void>;
}
/**
* Type guard to check if a cache key is a SpecificLinkCacheKey
*/
export declare function isSpecificLinkCacheKey(key: CacheKey): key is SpecificLinkCacheKey;
