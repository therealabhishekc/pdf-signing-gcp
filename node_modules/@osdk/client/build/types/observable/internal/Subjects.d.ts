import type { Logger } from "@osdk/api";
import { BehaviorSubject } from "rxjs";
import type { KnownCacheKey } from "./KnownCacheKey.js";
import { type Layers } from "./Layers.js";
import type { SubjectPayload } from "./SubjectPayload.js";
export declare class Subjects {
	#private;
	logger?: Logger;
	constructor({ logger, layers }: {
		logger?: Logger
		layers: Layers
	});
	peek: <KEY extends KnownCacheKey>(cacheKey: KEY) => BehaviorSubject<SubjectPayload<KEY>> | undefined;
	get: <KEY extends KnownCacheKey>(cacheKey: KEY) => BehaviorSubject<SubjectPayload<KEY>>;
	delete: <KEY extends KnownCacheKey>(cacheKey: KEY) => void;
}
