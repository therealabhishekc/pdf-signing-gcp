import type { InterfaceHolder } from "../../../object/convertWireToOsdkObjects/InterfaceHolder.js";
import type { ObjectHolder } from "../../../object/convertWireToOsdkObjects/ObjectHolder.js";
import type { BatchContext } from "../BatchContext.js";
import type { Canonical } from "../Canonical.js";
import type { ObjectCacheKey } from "../object/ObjectCacheKey.js";
/**
* Strategy interface for collection sorting
*/
export interface SortingStrategy {
	/**
	* Sort an array of object cache keys
	* @param objectCacheKeys - Keys to sort
	* @param batch - Batch context for reading objects
	* @returns Sorted array of keys
	*/
	sortCacheKeys(objectCacheKeys: ObjectCacheKey[], batch: BatchContext): ObjectCacheKey[];
}
/**
* No-operation sorting strategy that preserves original order
*/
export declare class NoOpSortingStrategy implements SortingStrategy {
	sortCacheKeys(objectCacheKeys: ObjectCacheKey[], _batch: BatchContext): ObjectCacheKey[];
}
type ObjectInterfaceComparer = (a: ObjectHolder | InterfaceHolder | undefined, b: ObjectHolder | InterfaceHolder | undefined) => number;
/**
* Sorting strategy for OrderBy clauses
*/
export declare class OrderBySortingStrategy implements SortingStrategy {
	private readonly apiName;
	private readonly orderBy;
	private readonly sortFns;
	constructor(apiName: string, orderBy: Canonical<Record<string, "asc" | "desc" | undefined>>);
	sortCacheKeys(objectCacheKeys: ObjectCacheKey[], batch: BatchContext): ObjectCacheKey[];
}
/**
* Creates sort functions for an orderBy clause
* @param orderBy - The order by clause
* @returns Array of sort functions
*/
export declare function createOrderBySortFns(orderBy: Canonical<Record<string, "asc" | "desc" | undefined>>): ObjectInterfaceComparer[];
export {};
