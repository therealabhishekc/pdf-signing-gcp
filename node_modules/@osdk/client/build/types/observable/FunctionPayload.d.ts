import type { CompileTimeMetadata, QueryDefinition } from "@osdk/api";
import type { QueryReturnType } from "../queries/types.js";
import type { Status } from "./ObservableClient/common.js";
export interface FunctionPayload<T = unknown> {
	status: Status;
	result: T | undefined;
	lastUpdated: number;
	error?: Error;
}
export interface TypedFunctionPayload<Q extends QueryDefinition<any>> extends FunctionPayload<QueryReturnType<CompileTimeMetadata<Q>["output"]>> {}
