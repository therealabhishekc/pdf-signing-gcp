import type { Logger } from "@osdk/api";
import { BaseLogger } from "./BaseLogger.js";
export declare class MinimalLogger extends BaseLogger implements Logger {
	constructor(bindings?: Record<string, any>, options?: {
		level?: string
		msgPrefix?: string
	});
	createLogMethod(name: "trace" | "debug" | "info" | "warn" | "error" | "fatal", bindings: Record<string, any>): Logger.LogFn;
}
