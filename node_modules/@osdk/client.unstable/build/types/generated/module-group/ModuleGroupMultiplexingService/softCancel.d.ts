import { type ConjureContext } from "conjure-lite";
import type { JobId as _JobId } from "../__components.js";
/**
* Soft cancels a job by calling the client-provided soft-cancel endpoint on the module where the job is running.
* MGS will not terminate the job in this request. Note that if a cache key was provided, the job will not be
* cached after it is soft-cancelled.
*/
export declare function softCancel(ctx: ConjureContext, jobId: _JobId): Promise<void>;
