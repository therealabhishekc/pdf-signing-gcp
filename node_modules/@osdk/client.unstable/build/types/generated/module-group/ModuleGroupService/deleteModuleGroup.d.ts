import { type ConjureContext } from "conjure-lite";
import type { ModuleGroupName as _ModuleGroupName } from "../__components.js";
/**
* Deletes a module group.
*
* The authHeader must be for a service user.
*
* Returns a 403 if the auth header does not have "contour:remove-module-group" on the root module group node.
*/
export declare function deleteModuleGroup(ctx: ConjureContext, moduleGroupName: _ModuleGroupName): Promise<void>;
