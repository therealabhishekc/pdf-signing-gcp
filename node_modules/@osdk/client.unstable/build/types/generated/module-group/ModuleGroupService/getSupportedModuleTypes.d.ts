import { type ConjureContext } from "conjure-lite";
import type { SupportedModuleType as _SupportedModuleType } from "../__components.js";
/**
* Returns a set of enum values indicating the module types (spark module, containerized module, etc.) that are
* supported to be launched and managed.
*/
export declare function getSupportedModuleTypes(ctx: ConjureContext): Promise<Array<_SupportedModuleType>>;
