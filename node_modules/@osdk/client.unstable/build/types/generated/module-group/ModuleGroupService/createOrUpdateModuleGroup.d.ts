import { type ConjureContext } from "conjure-lite";
import type { ModuleGroupCreationOrUpdateRequest as _ModuleGroupCreationOrUpdateRequest, ModuleGroupName as _ModuleGroupName } from "../__components.js";
/**
* Creates a module group according to the provided group definition.
* If the group already exists, it updates its definition to the provided one.
*
* The authHeader must be for a service user.
*
* Returns a 403 if the auth header does not have "contour:create-module-group" on the root module group node.
*/
export declare function createOrUpdateModuleGroup(ctx: ConjureContext, moduleGroupName: _ModuleGroupName, moduleGroupCreationOrUpdateRequest: _ModuleGroupCreationOrUpdateRequest): Promise<void>;
