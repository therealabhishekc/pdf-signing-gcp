import { type ConjureContext } from "conjure-lite";
import type { AvailableModulesResponse as _AvailableModulesResponse, ModuleGroupName as _ModuleGroupName } from "../__components.js";
/**
* Returns the set of Spark modules in the group that are not currently being phased out and are responding to
* health checks.
*
* The authHeader must be for a service user.
*
* Returns a 403 if the auth header does not have "contour:get-module-group" on the root module group node.
*/
export declare function getAvailableSparkModules(ctx: ConjureContext, moduleGroupName: _ModuleGroupName): Promise<_AvailableModulesResponse>;
