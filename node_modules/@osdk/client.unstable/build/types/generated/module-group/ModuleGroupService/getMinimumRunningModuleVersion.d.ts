import { type ConjureContext } from "conjure-lite";
import type { ModuleGroupName as _ModuleGroupName, SlsVersion as _SlsVersion } from "../__components.js";
/**
* Returns the minimum running SLS version among all spark modules in the provided module group.
* For container modules, the version will only be returned if the `slsInfo` for the primary
* container in the group is provided; otherwise, an exception will be thrown. By default we set it to 1.0.0
* if not present.
*/
export declare function getMinimumRunningModuleVersion(ctx: ConjureContext, moduleGroupName: _ModuleGroupName): Promise<_SlsVersion | null | undefined>;
